<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MediaPembelajaran">

        <activity android:name=".QuizResultActivity" />
        <activity android:name=".LeaderboardSulitActivity" />
        <activity android:name=".LeaderboardSedangActivity" />
        <activity android:name=".LeaderboardMudahActivity" />

        <activity android:name=".SkorSulitActivity" />
        <activity android:name=".SkorSedangActivity" />
        <activity android:name=".SkorMudahActivity" />

        <activity android:name=".QuizSulitActivity" />
        <activity android:name=".QuizSedangActivity" />
        <activity android:name=".QuizMudahActivity" />

        <activity android:name=".LeaderboardLevelActivity" />
        <activity android:name=".SkorLevelActivity" />
        <activity android:name=".QuizLevelActivity" />

        <activity android:name=".MateriActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />

        <!-- LAUNCHER -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MediaPembelajaran">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>